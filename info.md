Github Branch link

Deploy + deploy

Modified Views List
events.views.py

1. dashboardClassView(ListView)
2. event_detailsView(DetailView)
3. event_formView(CreateView)
4. edit_eventView(UpdateView)
5. delete_eventView(DeleteView)

Admin Panel Credentials
Organizer Credentials
participants Credentials
